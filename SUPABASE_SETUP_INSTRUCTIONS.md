# تعليمات تطبيق Schema على Supabase

## خطوات التطبيق:

### 1. افتح Supabase Dashboard
انتقل إلى: https://app.supabase.com/project/zucxosbiegdfllyzkufo

### 2. افتح SQL Editor
- من القائمة الجانبية، اختر **SQL Editor**
- اضغط على **New Query**

### 3. انسخ محتوى الملف
افتح ملف `supabase-schema.sql` وانسخ كل محتواه

### 4. الصق في SQL Editor
الصق الكود الكامل في محرر SQL

### 5. نفّذ الاستعلامات
اضغط على زر **Run** أو اختصار `Ctrl+Enter`

### 6. تحقق من النجاح
يجب أن ترى رسالة "Success" خضراء

## التحقق من الجداول:

### افتح Table Editor
من القائمة الجانبية، اختر **Table Editor** وتأكد من وجود:

- ✅ `managers` - جدول المدراء
- ✅ `certificates` - جدول الشهادات
- ✅ `verification_history` - سجل التحقق
- ✅ `dashboard_visits` - زيارات Dashboard

## إنشاء مدير جديد (اختياري):

إذا أردت إضافة مدير حقيقي بدلاً من الافتراضي:

```sql
INSERT INTO managers (name, email, admin_username, admin_password, role)
VALUES (
  'Your Name',
  'your-email@example.com',
  'your-username',
  'hashed-password-here', -- استخدم bcrypt
  'super_admin'
);
```

## ملاحظات مهمة:

### 🔐 تشفير كلمات المرور
المدير الافتراضي يستخدم كلمة مرور مشفرة نموذجية. في التطبيق الحقيقي:

1. استخدم مكتبة `bcryptjs` في React:
```bash
npm install bcryptjs
npm install --save-dev @types/bcryptjs
```

2. قم بتشفير كلمات المرور قبل إدخالها:
```javascript
import bcrypt from 'bcryptjs';
const hashedPassword = await bcrypt.hash('your-password', 10);
```

### 🔒 Row Level Security (RLS)
الجداول محمية بـ RLS Policies:
- **القراءة**: متاحة للجميع للشهادات
- **الكتابة**: محصورة بالمدراء فقط
- يمكنك تعديل السياسات من **Authentication** > **Policies**

### 📊 Views الجاهزة
تم إنشاء Views مفيدة:
- `dashboard_stats` - إحصائيات عامة
- `recent_certificates` - آخر الشهادات

يمكنك استعلامها مباشرة:
```javascript
const { data } = await supabase.from('dashboard_stats').select('*');
```

## استكمال التطبيق:

بعد تطبيق Schema، سأقوم بـ:
1. ✅ تحديث `services/supabase.ts` بالـ Types الجديدة
2. ✅ تحديث `LoginPage.tsx` لإضافة خيار Admin
3. ✅ إنشاء `Dashboard/` folder بكل الصفحات
4. ✅ بناء Sidebar قابل للطي
5. ✅ إضافة CRUD operations

---

**هل قمت بتطبيق الـ SQL بنجاح؟** أخبرني لأبدأ بالخطوة التالية! 🚀
