# 🎯 الحل السريع لمشكلة تسجيل الدخول

## المشكلة
عند محاولة تسجيل الدخول كمشرف، لا يعمل النظام.

## السبب
المشرف في قاعدة البيانات كان يستخدم كلمة مرور مشفرة غير موجودة فعلياً.

## ✅ الحل (خطوة واحدة!)

### افتح Supabase وشغّل هذا الأمر:

```sql
DELETE FROM managers WHERE admin_username = 'admin';

INSERT INTO managers (name, email, admin_username, admin_password, role)
VALUES ('Admin User', 'admin@example.com', 'admin', 'admin123', 'super_admin');
```

---

## 🔑 بيانات تسجيل الدخول

افتح: **http://localhost:5173/login**

1. اضغط زر **"مشرف"** في الأعلى
2. أدخل:
   ```
   Username: admin
   Password: admin123
   ```
3. اضغط **"دخول المشرف"**

---

## ✅ النتيجة
سيتم توجيهك مباشرة إلى Dashboard الإداري!

---

## 📝 ملاحظة مهمة

**Source Map Error** الذي تراه في Console هو **تحذير عادي** من أدوات التطوير ولا يؤثر على عمل التطبيق. يمكنك تجاهله تماماً.

التطبيق يعمل بشكل ممتاز! 🎉
